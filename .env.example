# Copy this file to .env and fill in your Supabase project values.
# Find these in: Supabase Dashboard → Project Settings → API

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# SUPABASE_SERVICE_ROLE_KEY is only needed server-side (Netlify Functions).
# Add it in the Netlify dashboard under Environment Variables — never put it here.
